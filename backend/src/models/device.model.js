const Device = {
  // Device model schema definition
  // id: UUID
  // name: string
  // type: string (mikrotik, olt, etc.)
  // ip_address: string
  // username: string
  // password: string
  // location: text
  // status: string
  // created_at: timestamp
  // updated_at: timestamp
};

module.exports = Device;
